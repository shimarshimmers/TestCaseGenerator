module.exports={
  generateCharacter : function(number,charSet){
    let test="";
    let res="";
            for(var i=0;i<number;i++)
           {
                res= generateRequiredTestCase(dataType,min,max,size);
                test=test.concat(res,'\n');

           }
           return test;
        
 } };